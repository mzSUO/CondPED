# CondPED 项目开发守则

## 项目背景
- 这是一个统计遗传学 R 包，专注于多性状 GWAS 的条件概率建模。

## 行为约束
- **禁止读取大数据文件**：忽略所有 .rds, .rda 和 data/ 目录下的文件。
- **文档保护**：man/ 目录下的 .Rd 文件由 roxygen2 自动生成，请不要直接修改它们。修改 R/ 下的注释即可。
- **环境操作**：在修改 R 代码后，优先建议运行 `devtools::load_all()` 验证。
- **Git 守则**：在执行大规模修改前，请提醒我进行 git commit。

## 技术偏好
- 保持 R 代码风格简洁，注释请使用 roxygen2 格式。
- 如果涉及矩阵运算，优先考虑性能，但保持代码可读性。