% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asset-comparison.R
\name{.normalize_asset_result}
\alias{.normalize_asset_result}
\title{Normalise a raw ASSET-like result into the frozen schema}
\usage{
.normalize_asset_result(raw, beta, trait_names, two_sided = TRUE)
}
\arguments{
\item{raw}{Raw backend result.}

\item{beta}{Named numeric effect vector (aligned to \code{trait_names}).}

\item{trait_names}{Trait names.}
}
\value{
List with the frozen schema and status \code{"ok"}.
}
\description{
Understands two raw shapes:
\enumerate{
\item a list carrying a p-value under one of \code{p}, \code{p_value},
\code{p.value}, \code{pval}, \code{best_p} and the best trait subset under one of
\code{subset}, \code{best_subset}, \code{traits}, \code{trait_subset},
\code{selected_traits};
\item a data.frame with one row per searched subset, a p-value
column (\code{p.value}/\code{p_value}/\code{pval}/\code{p}) and one column per trait
marking membership (non-\code{NA}, non-\code{0}); the row with the smallest
p-value wins.
}
The positive/negative split is derived from the sign of the CondPED
effect estimates (\code{beta}), never from ASSET internals. Anything else
raises an error (mapped to status \code{"asset_failed"} by the caller).
}
\keyword{internal}
