% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joint-signal-effects.R
\name{.fit_joint_signal_effects}
\alias{.fit_joint_signal_effects}
\title{Joint GLS effects for a set of signal markers}
\usage{
.fit_joint_signal_effects(
  null_fit,
  G,
  signals,
  marker_ids = colnames(G),
  rank_tol = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{null_fit}{A \code{"condped_mt_null"} fit with \code{rotation}.}

\item{G}{\verb{n x p} genotype matrix.}

\item{signals}{Character vector (or column indices) of signal
markers, in the desired block order.}

\item{marker_ids}{Marker order of \code{G}.}

\item{rank_tol}{Tolerance forwarded to \code{\link[=.safe_inverse]{.safe_inverse()}}.}
}
\value{
A list with \code{signals}, \code{U}, \code{J}, \code{J_inv}, \code{beta} (K x m
matrix, dimnamed by signal and trait), \code{se} (K x m),
\code{covariance} (m x m x K array of per-signal blocks),
\code{joint_covariance} (the full Km x Km matrix), \code{rank},
\code{condition}, \code{used_pseudoinverse} and \code{status}.
}
\description{
Computes \eqn{U_S = Z_S^\top P y}, \eqn{J_S = Z_S^\top P Z_S} for
the joint marker set with the fixed null projection (empty
conditioning set, i.e. the original P), then
\eqn{\widehat\beta_S = J_S^+ U_S} and \eqn{J_S^+}.
}
\keyword{internal}
