% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset-solutions.R
\name{.extract_irreducible_modules}
\alias{.extract_irreducible_modules}
\title{Extract all irreducible trait modules from one subset table}
\usage{
.extract_irreducible_modules(tab, tolerance)
}
\arguments{
\item{tab}{Complete per-locus subset table.}

\item{tolerance}{Numeric scalar in [0, 1).}
}
\value{
A data.frame with columns \code{tolerance}, \code{module_id},
\code{trait_set} (list-column), \code{trait_key}, \code{module_size},
\code{complement_set} (list-column), \code{complement_key},
\code{complement_loss}, \code{status}.
}
\description{
A non-empty set T is an irreducible module when removing it makes
representation infeasible (\verb{rho(A \\ T) > tolerance}) while no
proper subset of T has that property (inclusion-minimality). All
modules are returned, including singleton, joint and multiple
mutually non-containing modules. Requires a complete subset table
(\code{subset_mode = "all"}): the complement loss of every candidate
module must be present. NA (unusable) losses never qualify a module.
}
\keyword{internal}
