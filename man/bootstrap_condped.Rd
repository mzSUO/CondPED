% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap-condped.R
\name{bootstrap_condped}
\alias{bootstrap_condped}
\title{Parametric bootstrap for the CondPED pipeline}
\usage{
bootstrap_condped(
  object,
  Y = NULL,
  W = NULL,
  G = NULL,
  loci = NULL,
  B = 200L,
  type = c("parametric", "residual"),
  refit_null = TRUE,
  reselect = FALSE,
  interval = c("percentile", "bca"),
  seed = 1L,
  workers = 1L,
  min_valid = 0.8
)
}
\arguments{
\item{object}{A fitted CondPED object (e.g. from \code{\link[=condped]{condped()}}) or a
\code{"condped_mt_null"} fit.}

\item{Y}{Optional numeric \verb{n x m} phenotype matrix.}

\item{W}{Optional numeric \verb{n x q} fixed-effect design matrix.}

\item{G}{Optional numeric \verb{n x p} genotype dosage matrix.}

\item{loci}{Optional vector of loci to bootstrap; required when \code{object}
does not carry a selection.}

\item{B}{Integer; number of bootstrap replicates.}

\item{type}{Bootstrap type: \code{"parametric"} or \code{"residual"}.}

\item{refit_null}{Logical; re-estimate the variance components and the
conditional projection coefficients in every replicate.}

\item{reselect}{Logical; rerun the full pipeline (including selection) in
every replicate.}

\item{interval}{Confidence interval type: \code{"percentile"} or \code{"bca"}.}

\item{seed}{Random seed.}

\item{workers}{Integer; number of parallel workers.}

\item{min_valid}{Minimum fraction of valid replicates; below this
threshold the result carries status code \code{"bootstrap_failed"}.}
}
\value{
A list with components \code{replicates}, \code{intervals} (data.frame),
optional \code{selection_frequency}, \code{status} and \code{diagnostics} (including
\code{B}, \code{n_valid}, \code{refit_null}, \code{reselect}), as specified in the
interface contract.
}
\description{
Propagates the parameter uncertainty of the fitted null model, effects,
conditional deviations and PVE through parametric (or residual)
bootstrap. With \code{reselect = FALSE} the reported loci are held fixed; with
\code{reselect = TRUE} the full pipeline is rerun on each replicate to assess
selection stability.
}
\section{Status}{

S0 skeleton: this function is an API stub and currently throws an error of
class \code{condped_not_implemented}.
}

