% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-mt-null.R
\name{.reml_value_grad}
\alias{.reml_value_grad}
\title{Rotated REML/ML objective with analytic gradient}
\usage{
.reml_value_grad(
  theta,
  m,
  lambda,
  Y_tilde,
  W_tilde,
  chol_floor = 1e-08,
  reml = TRUE,
  need_grad = TRUE
)
}
\arguments{
\item{theta}{Log-Cholesky parameters: first \code{m(m+1)/2} for \code{Sigma_G},
then the same number for \code{Sigma_E}.}

\item{m}{Number of traits.}

\item{lambda}{Eigenvalues of \code{K} (length n, non-negative).}

\item{Y_tilde}{Rotated phenotypes \verb{U'Y} (\verb{n x m}).}

\item{W_tilde}{Rotated design \verb{U'W} (\verb{n x q}).}

\item{chol_floor}{Log-Cholesky diagonal floor.}

\item{reml}{Logical; \code{TRUE} (default) computes the restricted
log-likelihood, \code{FALSE} the full (profile) ML log-likelihood.}

\item{need_grad}{Logical; skip gradient assembly when \code{FALSE}.}
}
\value{
List with \code{value} (scalar log-likelihood) and \code{gradient}
(numeric vector, \code{NULL} when \code{need_grad = FALSE}; both are
\code{-1e10} / zeros for numerically illegal parameters).
}
\description{
Shared workhorse behind \code{\link[=.reml_objective_rotated]{.reml_objective_rotated()}} and the optimiser
interface. The gradient uses the standard REML derivative
\eqn{\partial \ell / \partial \theta = -\tfrac12 [\operatorname{tr}(P
\, \partial V) - y^\top P \, \partial V \, P y]}, evaluated block-wise in
the rotated space: with \eqn{D_j = M_j (X^\top V^{-1} X)^{-1} M_j^\top},
\eqn{B_j = V_j^{-1} - V_j^{-1} D_j V_j^{-1}} and \eqn{s_j = V_j^{-1}
\widetilde r_j}, every parameter of the log-Cholesky factor
\eqn{\partial \Sigma = E L^\top + L E^\top} gives
\eqn{\operatorname{tr}(B_j \partial \Sigma) = 2 (L^\top B_j)_{b,a}} and
\eqn{s^\top \partial \Sigma s = 2 s_a (L^\top s)_b}.
}
\keyword{internal}
