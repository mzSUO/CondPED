% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-mt-null.R
\name{.reml_objective_rotated}
\alias{.reml_objective_rotated}
\title{Rotated restricted (or full) log-likelihood of the multi-trait null model}
\usage{
.reml_objective_rotated(
  theta,
  m,
  lambda,
  Y_tilde,
  W_tilde,
  chol_floor = 1e-08,
  reml = TRUE
)
}
\arguments{
\item{theta}{Log-Cholesky parameters: first \code{m(m+1)/2} for \code{Sigma_G},
then the same number for \code{Sigma_E}.}

\item{m}{Number of traits.}

\item{lambda}{Eigenvalues of \code{K} (length n, non-negative).}

\item{Y_tilde}{Rotated phenotypes \verb{U'Y} (\verb{n x m}).}

\item{W_tilde}{Rotated design \verb{U'W} (\verb{n x q}).}

\item{chol_floor}{Log-Cholesky diagonal floor.}

\item{reml}{Logical; \code{TRUE} (default) computes the restricted
log-likelihood, \code{FALSE} the full (profile) ML log-likelihood.}
}
\value{
Scalar log-likelihood (higher is better); \code{-1e10} for
numerically illegal parameters.
}
\description{
Evaluates the (restricted) log-likelihood of
\eqn{y = X_0 b + u + \varepsilon},
\eqn{V = \Sigma_G \otimes K + \Sigma_E \otimes I_n}, in the eigen-rotated
space where coordinate \eqn{j} has covariance
\eqn{V_j = \lambda_j \Sigma_G + \Sigma_E}. The REML version includes the
fixed-effect correction \eqn{\log|X_0^\top V^{-1} X_0|}. The full
\eqn{nm \times nm} matrices are never constructed.
}
\keyword{internal}
