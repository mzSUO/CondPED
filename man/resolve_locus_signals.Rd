% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve-locus-signals.R
\name{resolve_locus_signals}
\alias{resolve_locus_signals}
\title{Resolve locus signals and estimate their joint effects}
\usage{
resolve_locus_signals(
  null_fit,
  G,
  locus_object,
  marker_ids = colnames(G),
  signal_adjust = c("within_locus_bonferroni", "fixed", "none"),
  alpha_signal = 0.05,
  fixed_p_threshold = NULL,
  max_signals = 10L,
  rank_tol = sqrt(.Machine$double.eps),
  return_conditional_scan = FALSE
)
}
\arguments{
\item{null_fit}{The single global null fit (with rotation).}

\item{G}{\verb{n x p} genotype matrix.}

\item{locus_object}{A \code{\link[=define_associated_loci]{define_associated_loci()}} result (or a list
with \code{loci} and \code{membership} data.frames).}

\item{marker_ids}{Marker order of \code{G}.}

\item{signal_adjust}{Local screening rule:
\code{"within_locus_bonferroni"}, \code{"fixed"} or \code{"none"}.}

\item{alpha_signal}{Level for the bonferroni/none rules.}

\item{fixed_p_threshold}{Raw-p threshold; required when
\code{signal_adjust = "fixed"}.}

\item{max_signals}{Maximum signals per locus.}

\item{rank_tol}{Tolerance forwarded to \code{\link[=.safe_inverse]{.safe_inverse()}}.}

\item{return_conditional_scan}{Logical; keep the per-step
conditional scan tables.}
}
\value{
A list with components
\describe{
\item{signals}{data.frame, one row per resolved signal:
\code{locus_id}, \code{signal_order}, \code{representative_snp},
\code{conditioning_key}, \code{conditional_p_raw},
\code{conditional_p_adjusted}, \code{screening_rule}, \code{M_remaining},
\code{selection_status}, \code{marginal_p}.}
\item{beta}{Long data.frame: \code{locus_id}, \code{signal_order},
\code{representative_snp}, \code{trait}, \code{beta}, \code{se} from the final
joint model.}
\item{covariance}{Named list per \code{locus_id}: the full joint
\verb{(Km) x (Km)} covariance matrix of that locus's signal effects
(blocks ordered by \code{signal_order}).}
\item{conditional_scan}{Per-locus stepwise scan history, or
\code{NULL}.}
\item{status}{Standard status list.}
\item{diagnostics}{List with \code{n_loci}, \code{n_signals},
\code{stop_reasons}, \code{n_rank_deficient}.}
}
}
\description{
Orchestrates the locus-level signal resolution: for each locus of a
\code{\link[=define_associated_loci]{define_associated_loci()}} result, the lead SNP is the first
representative; the stepwise conditional core
\code{\link[=.resolve_one_locus_signals]{.resolve_one_locus_signals()}} selects further representatives;
the final signal-specific effects come from ONE joint GLS refit of
the selected set via \code{\link[=.fit_joint_signal_effects]{.fit_joint_signal_effects()}} (the same
path used by \code{\link[=estimate_mt_effects]{estimate_mt_effects()}} with conditioning sets).
Stepwise temporary conditional betas never appear in the output.
}
