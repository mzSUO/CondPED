% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional.R
\name{compute_conditional_phenotype}
\alias{compute_conditional_phenotype}
\title{构造条件表型矩阵}
\usage{
compute_conditional_phenotype(Y_residual, V = NULL, tol_orthogonality = 0.01)
}
\arguments{
\item{Y_residual}{数值矩阵，n × m。残差表型矩阵。
\strong{要求}：已通过 \code{lm} 或 QTLNetwork 去除群体均值和协变量效应，
但保留遗传效应（目的是估计 V 的完整协方差结构，包含遗传协方差）。}

\item{V}{m × m 表型协方差矩阵（可选）。
若为 \code{NULL}（默认），由函数内部用 \code{Y_residual} 估计。
若已在外部计算，可直接传入以提高效率（多次调用时避免重复估计）。}

\item{tol_orthogonality}{正交性校验容差。|Cov| 超过此阈值时给出警告。默认 0.01。}
}
\value{
列表，包含：
\item{Y_cond}{n × m 矩阵，条件表型，列名与 \code{Y_residual} 一致}
\item{gamma}{长度为 m 的列表，\code{gamma[[i]]} 为性状 i 的投影系数向量}
\item{V}{使用的 m × m 表型协方差矩阵}
\item{schur}{长度为 m 的数值向量，各性状的 Schur 补（理论条件方差）}
\item{diagnostics}{诊断信息数据框，含正交性和方差等价性的验证结果}
}
\description{
对每个性状 i，构造条件表型：
\deqn{\tilde{y}^*_{ijk} = \tilde{y}_{ijk} - \tilde{\mathbf{y}}_{-i,jk}^\top \gamma_{i,-i}}
}
\details{
该构造确保 \eqn{\tilde{y}^*_i} 与其余所有性状在表型协方差意义上正交，
即 \eqn{\mathrm{Cov}(\tilde{y}^*_i, \tilde{y}_j) = 0}（对所有 j ≠ i）。

对条件表型估计的遗传效应即为\strong{条件遗传效应} \eqn{\theta_{iq}^{\mathrm{cond}}}，
代表不依赖于其他性状线性预测的独立遗传成分。
}
\examples{
\dontrun{
  # 生成示例数据（ρ = 0.5 的双性状场景）
  set.seed(42)
  n  <- 500
  Sigma <- matrix(c(1, 0.5, 0.5, 1), 2, 2)
  Y  <- MASS::mvrnorm(n, mu = c(0, 0), Sigma = Sigma)
  colnames(Y) <- c("A", "B")

  result <- compute_conditional_phenotype(Y)

  # 验证正交性：应接近 0
  cov(result$Y_cond[, "A"], Y[, "B"])

  # 验证方差等价：应接近 Schur 补
  var(result$Y_cond[, "A"])
  result$schur["A"]
}

}
\references{
Zhu, J. (1995). Analysis of conditional genetic effects and variance
components in developmental genetics. \emph{Genetics}, 141(4), 1633–1639.

Anderson, T. W. (1958). \emph{An Introduction to Multivariate Statistical
Analysis}. Wiley.
}
