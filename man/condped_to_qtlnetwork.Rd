% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qtlnetwork.R
\name{condped_to_qtlnetwork}
\alias{condped_to_qtlnetwork}
\title{condped_to_qtlnetwork
Bridge simulation.R output to QTXNetwork input.}
\usage{
condped_to_qtlnetwork(
  sim_result,
  geno_output_prefix,
  pheno_output_prefix,
  population = c("RIL", "F2")
)

condped_to_qtlnetwork(
  sim_result,
  geno_output_prefix,
  pheno_output_prefix,
  population = c("RIL", "F2")
)
}
\arguments{
\item{sim_result}{Output from generate_condped() or generate_class*().}

\item{geno_output_prefix}{Prefix for .gen.}

\item{pheno_output_prefix}{Prefix for .phe.}

\item{population}{"RIL" (default) or "F2".}
}
\value{
NULL (invisible)

NULL (invisible)
}
\description{
simulation.R RIL output is already -1/1 coded, so no conversion needed.
All markers placed on a single virtual chromosome for GWAS.

simulation.R RIL output is already -1/1 coded, so no conversion needed.
All markers placed on a single virtual chromosome for GWAS.
}
