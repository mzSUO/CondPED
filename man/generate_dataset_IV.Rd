% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_data.R
\name{generate_dataset_IV}
\alias{generate_dataset_IV}
\title{Dataset IV：三性状因果拓扑，验证 pairwise MR vs MVMR（补充模拟）}
\usage{
generate_dataset_IV(
  n,
  p = 1000L,
  topology = c("chain", "confound_obs", "confound_latent"),
  tau_AB = 0.4,
  tau_BC = 0.4,
  tau_CA = 0.5,
  tau_CB = 0.5,
  tau_UA = 0.5,
  tau_UB = 0.5,
  conf_pve = 0.03,
  class_pve = 0.05,
  iv_pve = NULL,
  n_iv = 20L,
  n_conf = 10L,
  rho = 0,
  maf = 0.3,
  population = "RIL",
  seed = NULL
)
}
\arguments{
\item{n}{整数。样本量（保持与主模拟一致）。}

\item{p}{整数。SNP数，默认1000。}

\item{topology}{字符串。"chain"/"confound_obs"/"confound_latent"。}

\item{tau_AB}{数值。chain拓扑中A→B因果效应，默认0.4。}

\item{tau_BC}{数值。chain拓扑中B→C因果效应，默认0.4。}

\item{tau_CA}{数值。confound拓扑中C→A的效应，默认0.5。}

\item{tau_CB}{数值。confound拓扑中C→B的效应，默认0.5。}

\item{tau_UA, tau_UB}{数值。latent拓扑中U→A和U→B的效应，默认0.5。}

\item{conf_pve}{数值。混杂工具变量单点PVE，默认0.03。
n_conf × conf_pve必须小于0.9。}

\item{class_pve}{数值。性状特异位点PVE，默认0.05。}

\item{iv_pve}{数值或NULL。清洁工具变量PVE，NULL=自动。}

\item{n_iv}{整数。清洁工具变量数，默认20。}

\item{n_conf}{整数。混杂工具变量数，默认10。}

\item{rho, maf, population, seed}{同其他函数。}
}
\value{
命名列表：Y(n×3), G, eps, X, truth, dataset="IV", topology,
Tau_full, Tau_obs(3×3), tau_AB_true, latent(逻辑值), seed, h2_obs, iv_pve_used。
}
\description{
【验证目的】
对比三种因果拓扑下，两两MR和MVMR的估计偏差：
\enumerate{
\item chain（因果链 A→B→C）：无共同上游，两两MR理论无偏，验证良性情形
\item confound_obs（C→A，C→B，真值tau_AB=0）：可观测混杂，两两MR产生假因果
\item confound_latent（U→A，U→B，U不可测）：潜在混杂，两种方法均无法纠正
}
}
\details{
【位点布局（按拓扑不同）】
chain：
IV_A（n_iv个）：仅影响A（清洁工具变量）
IV_B（n_iv个）：仅影响B（清洁工具变量）
Cspecific（5个）：仅影响C
confound_obs：
IV_C_confounder（n_conf个）：直接影响混杂因子C（污染工具变量）
IV_A_clean（n_iv个）：清洁工具变量
IV_B_clean（n_iv个）：清洁工具变量
confound_latent：
IV_U_latent（n_conf个）：影响潜在混杂U（不可观测）
IV_A_clean（n_iv个）：清洁工具变量
IV_B_clean（n_iv个）：清洁工具变量
Cspecific（5个）：仅影响C

【安全约束】
n_conf × conf_pve < 1，否则方差标准化失败（残差方差为负）
}
