% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan-mt-omnibus.R
\name{scan_mt_omnibus}
\alias{scan_mt_omnibus}
\title{Multi-trait omnibus score scan}
\usage{
scan_mt_omnibus(
  null_fit,
  G,
  marker_ids = colnames(G),
  maf_min = 0.05,
  chunk_size = 2000L,
  rank_tol = sqrt(.Machine$double.eps),
  return_score = FALSE,
  return_effects = FALSE,
  bootstrap_p = FALSE,
  n_boot = 0L,
  seed = NULL
)
}
\arguments{
\item{null_fit}{An object of class \code{"condped_mt_null"} returned by
\code{\link[=fit_mt_null]{fit_mt_null()}}.}

\item{G}{Numeric \verb{n x p} genotype dosage matrix coded 0/1/2.}

\item{marker_ids}{Character vector of marker identifiers; defaults to
\code{colnames(G)}.}

\item{maf_min}{Minimum minor allele frequency; markers below the threshold
are filtered.}

\item{chunk_size}{Integer; number of markers processed per block.}

\item{rank_tol}{Relative tolerance used for the rank and generalised
inverse of \eqn{J_l}.}

\item{return_score}{Logical; also return the score vectors.}

\item{return_effects}{Logical; also return marker effect estimates.}

\item{bootstrap_p}{Logical; compute bootstrap p-values.}

\item{n_boot}{Integer; number of bootstrap replicates when
\code{bootstrap_p = TRUE}.}

\item{seed}{Optional random seed.}
}
\value{
A list with components \code{omnibus} (a data.frame with columns
\code{marker_id}, \code{maf}, \code{n_eff}, \code{Q}, \code{df}, \code{p_value}, \code{rank_J},
\code{condition_J}, \code{status}, plus \code{filter_reason} for filtered markers),
optional \code{score} and \code{effects}, plus \code{status} and \code{diagnostics}, as
specified in the interface contract.
}
\description{
Runs the joint multi-trait score test for every marker, strictly following
equations (9)-(12) of the Methods: \eqn{U_l = Z_l^\top P y},
\eqn{J_l = Z_l^\top P Z_l}, \eqn{Q_l = U_l^\top J_l^+ U_l}. The full
\eqn{Z_l} and \eqn{P} are never constructed explicitly; block computations
use the rotation object of the null fit.
}
