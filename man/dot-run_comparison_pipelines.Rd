% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparison-pipelines.R
\name{.run_comparison_pipelines}
\alias{.run_comparison_pipelines}
\title{Run the three Stage-6C comparison pipelines}
\usage{
.run_comparison_pipelines(
  fit,
  G,
  locus_object,
  scan,
  trait_names,
  alpha_signal = 0.05,
  signal_adjust = "within_locus_bonferroni",
  max_signals = 10L,
  asset_backend = NULL,
  tolerance = 0.1,
  ...
)
}
\arguments{
\item{fit}{The single global null fit (with rotation).}

\item{G}{\verb{n x p} genotype dosage matrix with marker-id colnames.}

\item{locus_object}{A \code{\link[=define_associated_loci]{define_associated_loci()}} result (or a list
with \code{loci} and \code{membership} data.frames; \code{loci} must carry
\code{locus_id}, \code{lead_snp}, \code{lead_p}).}

\item{scan}{A \code{\link[=scan_mt_omnibus]{scan_mt_omnibus()}} result or its omnibus data.frame;
used to annotate lead omnibus p-values.}

\item{trait_names}{Trait names; must match
\code{colnames(fit$Sigma_P_ref)} (canonical order is taken from the
fit).}

\item{alpha_signal}{Signal-level alpha: forwarded to the resolution
screening rule and used as the within-signal Holm level in the
CondPED-full attribution.}

\item{signal_adjust}{Local screening rule for the resolution.}

\item{max_signals}{Maximum signals per locus in the resolution.}

\item{asset_backend}{Backend hook forwarded to
\code{\link[=.run_asset_comparison]{.run_asset_comparison()}} (test injection point for a fake
ASSET).}

\item{tolerance}{Primary representation-loss tolerance for the
CondPED-full decomposition.}

\item{...}{Forwarded to \code{\link[=resolve_locus_signals]{resolve_locus_signals()}} (e.g. \code{rank_tol}).}
}
\value{
A list with components
\describe{
\item{lead_asset}{Result of \code{\link[=.pipeline_lead_asset]{.pipeline_lead_asset()}}.}
\item{resolved_asset}{Result of \code{\link[=.pipeline_resolved_asset]{.pipeline_resolved_asset()}}.}
\item{condped_full}{Result of \code{\link[=.pipeline_condped_full]{.pipeline_condped_full()}}.}
\item{status}{\code{"ok"} when all three pipeline statuses are \code{"ok"},
otherwise the sorted unique non-ok codes joined by \code{";"}.}
}
}
\description{
Orchestrates the lead-ASSET, resolved-ASSET and CondPED-full
comparison pipelines over a common locus set. The signal resolution
(\code{\link[=resolve_locus_signals]{resolve_locus_signals()}}) is computed once and shared by the
resolved-ASSET and CondPED-full pipelines. The trait z-score
correlation matrix handed to ASSET is \code{cov2cor(fit$Sigma_P_ref)}
(correlated traits supported; never diagonalised). ASSET itself is
optional: with \code{asset_backend = NULL} and ASSET not installed, the
ASSET pipelines report status \code{"asset_not_available"} without
erroring.
}
\keyword{internal}
