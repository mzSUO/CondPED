% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve-locus-signals.R
\name{.select_next_signal}
\alias{.select_next_signal}
\title{Select the next signal from one conditional scan}
\usage{
.select_next_signal(
  tab,
  chromosome,
  position,
  screening_rule,
  alpha_signal,
  fixed_p_threshold,
  M_remaining
)
}
\arguments{
\item{tab}{Conditional scan table (marker_id, Q, df, p_value,
status).}

\item{chromosome, position}{Named marker annotation vectors.}

\item{screening_rule}{One of \code{"within_locus_bonferroni"}, \code{"fixed"},
\code{"none"}.}

\item{alpha_signal}{Level for the bonferroni/none rules.}

\item{fixed_p_threshold}{Raw-p threshold for the \code{"fixed"} rule.}

\item{M_remaining}{Eligible candidate count fixed before the scan.}
}
\value{
A list with \code{found}, \code{marker_id}, \code{p_raw}, \code{p_adjusted},
\code{n_passing}, \code{M_remaining}.
}
\description{
Applies the local screening rule and picks the winner
deterministically. \code{M_remaining} is fixed BEFORE the scan (the
number of eligible remaining regional QC markers) and is never
redefined by the resulting p-values. Only markers with a usable
conditional statistic (finite Q and p-value) can win.
}
\keyword{internal}
