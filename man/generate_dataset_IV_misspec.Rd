% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_data.R
\name{generate_dataset_IV_misspec}
\alias{generate_dataset_IV_misspec}
\title{Dataset IV-misspec：四性状，验证MVMR错误指定的失效（补充模拟）}
\usage{
generate_dataset_IV_misspec(
  n,
  p = 1000L,
  tau_CA = 0.5,
  tau_CB = 0.5,
  tau_CD = 0.5,
  conf_pve = 0.04,
  iv_pve = NULL,
  n_iv = 20L,
  n_conf = 15L,
  rho = 0,
  maf = 0.3,
  population = "RIL",
  seed = NULL
)
}
\arguments{
\item{n, p}{样本量和SNP数。}

\item{tau_CA}{数值。C→A效应，默认0.5。}

\item{tau_CB}{数值。C→B效应，默认0.5。}

\item{tau_CD}{数值。C→D效应（诱饵），默认0.5。}

\item{conf_pve}{数值。混杂工具变量单点PVE，默认0.04。}

\item{iv_pve}{数值或NULL。清洁工具变量PVE，NULL=自动。}

\item{n_iv}{整数。每个清洁块的工具变量数，默认20。}

\item{n_conf}{整数。混杂工具变量数，默认15。}

\item{rho, maf, population, seed}{同其他函数。}
}
\value{
命名列表：Y(n×4), G, eps, X, truth, dataset="IV_misspec", Tau_full,
tau_AB_true=0, seed, h2_obs, iv_pve_used。
}
\description{
【验证目的】
证明MVMR的纠偏能力完全依赖正确指定混杂因子：
\itemize{
\item MVMR正确指定C（真混杂）→ Type I error ≈ 5\%（无偏）
\item MVMR错误指定D（诱饵，是C的下游）→ Type I error ≈ 97\%（与两两MR相当）
}
}
\details{
【性状角色】
A=1：暴露性状（待估计A→B）
B=2：结局性状（tau_AB=0，无真实因果）
C=3：真混杂（C→A，C→B，产生A和B的假相关）
D=4：诱饵（C→D，和系统相关但不是A/B的共同上游）

【关键参数安全约束】n_conf × conf_pve < 0.9
}
