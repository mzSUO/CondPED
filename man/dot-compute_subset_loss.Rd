% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset-loss.R
\name{.compute_subset_loss}
\alias{.compute_subset_loss}
\title{Conditional representation loss of one representing subset}
\usage{
.compute_subset_loss(
  beta,
  Sigma,
  representing_set,
  trait_names = names(beta),
  inverse_tol = sqrt(.Machine$double.eps),
  qform_tol = 1e-12,
  decomposition_rel_tol = 1e-08,
  condition_number_warning = 1e+10
)
}
\arguments{
\item{beta}{Named numeric vector of locus effects on the candidate
traits; \code{names(beta)} defines the candidate set A.}

\item{Sigma}{Numeric symmetric \verb{length(beta) x length(beta)}
reference covariance matrix. If dimnamed, it is reordered to match
\code{names(beta)}; otherwise the order of \code{beta} is assumed.}

\item{representing_set}{Subset of the candidate traits (character
names or integer positions); may be empty.}

\item{trait_names}{Reference trait order; defaults to \code{names(beta)}.}

\item{inverse_tol}{Eigenvalue tolerance for all inverses, ranks and
condition numbers (via \code{\link[=.safe_inverse]{.safe_inverse()}}).}

\item{qform_tol}{Energy threshold; \code{full_qform <= qform_tol} yields
\code{not_applicable}.}

\item{decomposition_rel_tol}{Maximum tolerated relative error of the
quadratic-form decomposition before the result is \code{unstable}.}

\item{condition_number_warning}{Condition numbers above this value
are recorded as warnings.}
}
\value{
A list with components \code{representing_set},
\code{representing_key}, \code{complement_set}, \code{complement_key}, \code{Gamma},
\code{eta}, \code{Omega}, \code{full_qform}, \code{subset_qform}, \code{residual_qform},
\code{representation_loss} (the formal loss; \code{NA} unless the result is
usable, see Details), \code{raw_representation_loss} (the untruncated
diagnostic value), \code{decomposition_error}, \code{rank_Sigma_SS},
\code{rank_Omega}, \code{condition_Sigma_SS}, \code{condition_Omega},
\code{used_pseudoinverse} and \code{status} (a standard status list with
code \code{"ok"}, \code{"rank_deficient"}, \code{"unstable"} or
\code{"not_applicable"}; \code{ok} is \code{FALSE} for \code{rank_deficient} and
\code{unstable}).
}
\description{
Given a locus effect vector on a candidate trait set A and a fixed
reference covariance matrix, computes the conditional representation
loss of the representing subset S: the fraction of the full
Mahalanobis quadratic form that the traits in R = A \\ S retain
after best-linear representation by S.
}
\details{
With Sigma blocked by (S, R):
\deqn{\Gamma_{R|S} = \Sigma_{SS}^{-1}\Sigma_{SR}}
\deqn{\eta_{R|S} = \beta_R - \Gamma_{R|S}^{\top}\beta_S}
\deqn{\Omega_{R|S} = \Sigma_{RR} - \Sigma_{RS}\Sigma_{SS}^{-1}\Sigma_{SR}}
\deqn{\rho(S) = \frac{\eta^{\top}\Omega^{-1}\eta}
  {\beta_A^{\top}\Sigma_{AA}^{-1}\beta_A}}

Boundary conventions: \code{rho(empty) = 1} (no Gamma constructed,
\code{subset_qform = 0}, \code{residual_qform = full_qform}) and
\code{rho(A) = 0} (\code{residual_qform = 0}, \code{subset_qform = full_qform}).
When \code{full_qform <= qform_tol} the locus carries no signal and the
result is \code{not_applicable} (a legitimate state: \code{status$ok} stays
\code{TRUE}). Only floating-point-scale excursions of
the loss outside the unit interval are truncated; grossly out-of-range values
are kept and marked \code{unstable}, never silently truncated.

Usability rule (frozen): a result may enter formal subset
optimisation only when every component inverse is numerically full
rank, the quadratic-form decomposition holds within
\code{decomposition_rel_tol} and the loss lies in the unit interval. If
any condition fails (\code{rank_deficient} or \code{unstable}),
\code{representation_loss} is \code{NA} and \code{status$ok} is \code{FALSE}; the
untruncated value remains available as \code{raw_representation_loss}
together with all other diagnostics. Rank-deficient inputs violate
the theoretical \code{Sigma > 0} assumption, so pseudo-inverse losses
are never offered as formal results.
}
\keyword{internal}
