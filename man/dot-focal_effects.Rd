% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-condped-data.R
\name{.focal_effects}
\alias{.focal_effects}
\title{Scaled focal-QTL effects with exact projection identities}
\usage{
.focal_effects(
  architecture,
  m,
  target_trait,
  gamma,
  delta,
  v_qtl,
  locus_pve,
  scale = 1
)
}
\arguments{
\item{architecture}{Architecture label.}

\item{m}{Number of traits.}

\item{target_trait}{Target trait index.}

\item{gamma}{True projection coefficients (\verb{m x (m-1)}).}

\item{delta}{Deviation effect (template units).}

\item{v_qtl}{Expected genotype variances of the focal loci.}

\item{locus_pve}{Target average PVE (scalar or vector; the mean is used).}

\item{scale}{Additional scalar factor applied to all loci (used by the
PSD shrinkage fallback).}
}
\value{
List with \code{beta} (\verb{n_qtl x m} effect matrix) and \code{s} (per-locus
cumulative scaling factors, including \code{scale}).
}
\description{
Scales the effect template so that the average PVE over the
template-active traits equals the target (Methods eq. 69), and enforces
the exact conditional identities for the projection-based
architectures: \code{covariance_aligned} sets the target-trait effect to
exactly \eqn{\gamma^\top \beta_{-t}}; \code{conditional_deviation} adds
exactly \code{s_l * delta} on top.
}
\keyword{internal}
