% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparison-pipelines.R
\name{.pipeline_lead_asset}
\alias{.pipeline_lead_asset}
\title{Lead-ASSET pipeline: marginal lead-SNP effects through ASSET}
\usage{
.pipeline_lead_asset(fit, G, locus_object, omnibus, trait_names, asset_backend)
}
\arguments{
\item{fit}{The global null fit (with rotation).}

\item{G}{\verb{n x p} genotype matrix (must have marker-id colnames).}

\item{locus_object}{List with \code{loci} and \code{membership} data.frames.}

\item{omnibus}{Omnibus scan data.frame (from \code{\link[=scan_mt_omnibus]{scan_mt_omnibus()}}),
used only to annotate the lead's marginal omnibus p-value.}

\item{trait_names}{Trait names (order of \code{fit$Sigma_P_ref}).}

\item{asset_backend}{Backend hook for \code{\link[=.run_asset_comparison]{.run_asset_comparison()}}.

The ASSET correlation matrix is derived per locus from the SAME
marginal effect fit as beta/se: \code{cov2cor()} of the marginal
covariance block — never from \code{Sigma_P_ref}.}
}
\value{
List with \code{loci} (named list per \code{locus_id}; each entry has
\code{locus_id}, \code{lead_snp}, \code{lead_p}, \code{omnibus_p}, named \code{beta}/\code{se}
vectors, \code{Sigma_Z} and the frozen adapter fields), \code{n_loci} and
\code{status}.
}
\description{
For every locus, takes the locus lead SNP's MARGINAL trait-wise
beta/se from \code{\link[=estimate_mt_effects]{estimate_mt_effects()}} (no conditional resolution) and
runs the ASSET adapter once per locus.
}
\keyword{internal}
