% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_data.R
\name{compute_residual_variance}
\alias{compute_residual_variance}
\title{计算使 Var(Y_i)=1 的残差协方差矩阵}
\usage{
compute_residual_variance(B, Tau, var_x, rho = 0, max_iter = 200L, tol = 1e-08)
}
\arguments{
\item{B}{p×m SNP效应矩阵。B\link{l_i}=位点l对性状i的加性效应。}

\item{Tau}{m×m因果效应矩阵。Tau\link{j_i}=性状i对性状j的因果效应。}

\item{var_x}{数值。基因型方差（RIL:4×maf×(1-maf)）。}

\item{rho}{数值。残差相关系数，默认0（性状间残差独立）。}

\item{max_iter}{整数。最大迭代次数，默认200。}

\item{tol}{数值。收敛判断阈值，默认1e-8。}
}
\value{
m×m 残差协方差矩阵 Sigma_E。
}
\description{
【目的】确保所有性状的表型方差均为1，便于跨参数比较。
}
\details{
【数学原理】
对于无因果链模型（M1，tau=0）：
Sigma_Y = var_x × B'B + Sigma_E
令 diag(Sigma_Y) = 1，可直接解析得 diag(Sigma_E) = 1 - diag(var_x×B'B)

对于有因果链模型（M2，tau≠0）：
Sigma_Y = A' × (var_x×B'B + Sigma_E) × A
其中 A = (I - tau')^{-1}
需要迭代调整 diag(Sigma_E) 直到 diag(Sigma_Y) ≈ 1

【残差相关】rho>0时，性状间残差协方差 = rho × sqrt(sigma_i²×sigma_j²)
这模拟共同环境因素（如温度、施肥）导致的跨性状表型相关
}
