% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subset-solutions.R
\name{.enumerate_subsets}
\alias{.enumerate_subsets}
\title{Enumerate candidate representing subsets}
\usage{
.enumerate_subsets(
  A,
  mode = c("all", "singleton", "custom"),
  custom_sets = NULL
)
}
\arguments{
\item{A}{Normalised candidate trait set (character vector).}

\item{mode}{One of \code{"all"}, \code{"singleton"}, \code{"custom"}.}

\item{custom_sets}{Optional list of trait sets for \code{mode = "custom"}.}
}
\value{
A list with components \code{sets} (list of normalised character
vectors, unique by stable key) and \code{keys} (their string keys).
\code{"all"} returns all \verb{2^|A|} subsets ordered by size then trait
order; \code{"singleton"} returns the empty set, the full set, every
singleton and every singleton complement (duplicates removed, so
\verb{|A| = 2} yields 4 sets); \code{"custom"} returns the normalised
\code{custom_sets} plus the empty and full sets.
}
\description{
Enumerate candidate representing subsets
}
\keyword{internal}
