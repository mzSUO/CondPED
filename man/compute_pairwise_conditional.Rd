% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional.R
\name{compute_pairwise_conditional}
\alias{compute_pairwise_conditional}
\title{构造成对条件表型 ỹ*_{outcome | exposure}}
\usage{
compute_pairwise_conditional(
  Y_residual,
  exposure,
  outcome,
  V = NULL,
  tol_orthogonality = 0.01
)
}
\arguments{
\item{Y_residual}{n × m 残差表型矩阵（与 compute_conditional_phenotype 同口径）。}

\item{exposure}{暴露性状：列名（字符）或列索引（整数）。}

\item{outcome}{结局性状：列名（字符）或列索引（整数）。}

\item{V}{可选 m × m 表型协方差矩阵；NULL 时由 Y_residual 估计。}

\item{tol_orthogonality}{正交性校验容差。默认 0.01。}
}
\value{
列表：
\item{y_cond}{长度 n 的成对条件表型向量 ỹ*_{outcome|exposure}}
\item{gamma}{标量投影系数 Cov(out,exp)/Var(exp)}
\item{schur}{理论条件方差 V_out - Cov^2/V_exp}
\item{max_cov}{Cov(ỹ*, ỹ_exposure) 的绝对值，应接近 0（正交性校验）}
}
\description{
与 compute_conditional_phenotype()（全条件，对所有其他性状投影）不同，
本函数只把 outcome 对单一 exposure 投影。用于 Layer 3 MR 的排他性筛选：
在 m≥3 的多性状场景下，全条件投影会把共同上游混杂的信号一并消除，导致
混杂工具通过排他性筛选；成对投影只移除暴露方向的依赖，能保留并暴露
混杂工具经后门路径对结局的残余效应，从而将其挡在 IV 集合之外。
}
\details{
仅用 {exposure, outcome} 的 2×2 子矩阵，对任意性状数 m 成立。
}
\seealso{
\code{\link{compute_conditional_phenotype}}（全条件版本，供 Layer 2 分类）
}
