% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define-associated-loci.R
\name{define_associated_loci}
\alias{define_associated_loci}
\title{Define associated loci from selected markers}
\usage{
define_associated_loci(
  omnibus,
  G,
  chromosome,
  position,
  selected_markers,
  marker_ids = colnames(G),
  method = c("ld_clump", "physical", "custom"),
  window_bp = NULL,
  r2_threshold = NULL,
  locus_map = NULL,
  merge_overlaps = TRUE
)
}
\arguments{
\item{omnibus}{Omnibus scan result returned by \code{\link[=scan_mt_omnibus]{scan_mt_omnibus()}},
or its \code{omnibus} data.frame (columns \code{marker_id}, \code{p_value},
\code{status}).}

\item{G}{Numeric \verb{n x p} genotype dosage matrix, used for
\code{r^2} computations.}

\item{chromosome}{Length-\code{p} chromosome annotation per marker.}

\item{position}{Length-\code{p} physical position per marker (bp).}

\item{selected_markers}{Character vector of marker ids selected by
the genome-wide screen. Required; no default.}

\item{marker_ids}{Marker order of \code{chromosome}/\code{position}/columns
of \code{G}; defaults to \code{colnames(G)}.}

\item{method}{Locus construction method.}

\item{window_bp}{Window size in bp (required for \code{physical} and
\code{ld_clump}).}

\item{r2_threshold}{Squared-correlation threshold for \code{ld_clump}.}

\item{locus_map}{data.frame with columns \code{marker_id}, \code{locus_id}
(required for \code{custom}).}

\item{merge_overlaps}{Logical; merge overlapping intervals
(\code{physical} method).}
}
\value{
A list with components
\describe{
\item{loci}{data.frame with columns \code{locus_id} (deterministic
\code{chromosome:start-end}, built from member positions),
\code{chromosome}, \code{start}, \code{end}, \code{lead_snp}, \code{lead_p},
\code{n_significant_markers}, \code{n_region_markers}, \code{status}.}
\item{membership}{data.frame with columns \code{locus_id},
\code{marker_id}, \code{significant_in_marginal_scan}, \code{r2_to_lead},
\code{position}.}
\item{status}{Standard status list (\code{ok} or \code{empty_selection}).}
\item{diagnostics}{List with \code{method}, \code{n_selected},
\code{n_loci}, \code{n_members} and parameter echoes.}
}
}
\description{
Organises an explicitly supplied set of selected markers into
associated loci. Selected markers only CONSTRUCT loci and elect
lead SNPs; once a locus boundary is defined, membership includes
every QC-passed marker inside the region, including markers that
were not significant in the marginal scan. This function never
performs genome-wide selection itself.
}
\details{
Methods:
\describe{
\item{\code{physical}}{Per chromosome, each selected marker opens the
interval \verb{position +/- window_bp}; overlapping intervals are
merged when \code{merge_overlaps = TRUE}.}
\item{\code{ld_clump}}{Selected markers are processed in ascending
marginal p-value order: the best remaining marker becomes the
lead of a new clump; every remaining selected marker within
\code{window_bp} of the lead AND with squared genotype correlation
\code{r^2 >= r2_threshold} to the lead joins that clump. The locus
region is \verb{lead position +/- window_bp}.}
\item{\code{custom}}{\code{locus_map} (a data.frame with \code{marker_id} and
\code{locus_id}) defines membership directly; it must cover every
selected marker.}
}

For \code{physical} and \code{ld_clump}, membership is then every non-filtered
(QC-passed) marker in the omnibus table inside the region. The lead
SNP is the selected marker with the smallest marginal omnibus
p-value in the locus. Results are invariant to the input order of
markers.
}
