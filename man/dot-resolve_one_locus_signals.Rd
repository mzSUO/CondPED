% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve-locus-signals.R
\name{.resolve_one_locus_signals}
\alias{.resolve_one_locus_signals}
\title{Resolve the representative signals of one locus stepwise}
\usage{
.resolve_one_locus_signals(
  locus_id,
  lead_snp,
  members,
  null_fit,
  G,
  marker_ids = colnames(G),
  chromosome,
  position,
  lead_marginal_p = NA_real_,
  screening_rule = c("within_locus_bonferroni", "fixed", "none"),
  alpha_signal = 0.05,
  fixed_p_threshold = 0.05,
  max_signals = 5L,
  rank_tol = sqrt(.Machine$double.eps),
  keep_history = TRUE
)
}
\arguments{
\item{locus_id}{Locus identifier.}

\item{lead_snp}{Lead SNP marker id (first signal).}

\item{members}{Character vector of the locus's regional QC markers.}

\item{null_fit}{The single global null fit (with rotation).}

\item{G}{\verb{n x p} genotype matrix.}

\item{marker_ids}{Marker order of \code{G}.}

\item{chromosome, position}{Named marker annotation vectors.}

\item{lead_marginal_p}{Marginal omnibus p-value of the lead.}

\item{screening_rule}{Local screening rule.}

\item{alpha_signal}{Level for bonferroni/none rules.}

\item{fixed_p_threshold}{Raw-p threshold for the \code{"fixed"} rule.}

\item{max_signals}{Maximum number of signals per locus.}

\item{rank_tol}{Tolerance forwarded to \code{\link[=.safe_inverse]{.safe_inverse()}}.}

\item{keep_history}{Logical; keep each step's conditional scan.}
}
\value{
A list with \code{signals} (data.frame: \code{locus_id},
\code{signal_order}, \code{representative_snp},
\code{conditioning_before_selection} (list-column),
\code{conditioning_key}, \code{n_conditioning}, \code{conditional_p_raw},
\code{conditional_p_adjusted}, \code{screening_rule}, \code{M_remaining},
\code{selection_status}), \code{selected} (ordered signal marker ids),
\code{history} (per-step conditional scan tables, or \code{NULL}),
\code{status} and \code{diagnostics} (with \code{stop_reason}, \code{n_signals},
\code{n_steps}).
}
\description{
Fixed algorithm: the first signal is the locus lead SNP (marginal
evidence). Then repeat: condition on all selected signals, rescan
ALL remaining regional QC markers, apply the local screening rule,
pick the smallest conditional raw p (deterministic tie-break), stop
when nobody passes or \code{max_signals} is reached.
}
\keyword{internal}
