% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate-mt-effects.R
\name{estimate_mt_effects}
\alias{estimate_mt_effects}
\title{Multi-trait marker effect estimation by GLS}
\usage{
estimate_mt_effects(
  null_fit,
  G,
  loci,
  marker_ids = colnames(G),
  rank_tol = sqrt(.Machine$double.eps),
  return_covariance = TRUE
)
}
\arguments{
\item{null_fit}{An object of class \code{"condped_mt_null"} returned by
\code{\link[=fit_mt_null]{fit_mt_null()}}.}

\item{G}{Numeric \verb{n x p} genotype dosage matrix coded 0/1/2.}

\item{loci}{Vector of locus indices (integer positions in \code{G}) or marker
identifiers (matching \code{marker_ids}) to estimate.}

\item{marker_ids}{Character vector of marker identifiers; defaults to
\code{colnames(G)}.}

\item{rank_tol}{Relative tolerance used for the generalised inverse of
\eqn{J}.}

\item{return_covariance}{Logical; return the per-locus \verb{m x m} effect
covariance matrices.}
}
\value{
A list with components \code{effects_long} (a data.frame with columns
\code{marker_id}, \code{trait}, \code{beta}, \code{se}, \code{z}, \code{p_value}), \code{beta} (an
n_loci x m matrix with marker and trait dimnames), \code{covariance} (an
m x m x n_loci array), \code{se} (an n_loci x m matrix),
\code{genotype_variance} (named numeric vector of per-locus variances of
the non-missing centred genotypes), \code{locus_table} (a data.frame with
\code{marker_id}, \code{maf}, \code{genotype_variance}, \code{rank_J}, \code{condition_J},
\code{status}), \code{trait_names}, \code{status} and \code{diagnostics}, as specified
in the interface contract.
}
\description{
Estimates the multi-trait effects of selected loci by generalised least
squares, \eqn{\widehat\beta = J^+ U}, reusing the rotation object of the
null fit. The returned covariance equals \eqn{J^+}.
}
