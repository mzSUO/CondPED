% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate-condped-simulation.R
\name{.match_signals}
\alias{.match_signals}
\title{Match truth and estimated signals by genomic position}
\usage{
.match_signals(truth, estimates, G = NULL)
}
\arguments{
\item{truth}{Truth component with \code{signals} (and usually \code{loci}).}

\item{estimates}{Estimates list with \code{signals} and optionally
\code{positions} (named marker -> position vector).}

\item{G}{Optional n x p genotype matrix with marker-id column
names; when supplied, matching prefers the highest squared
genotype correlation (via \code{.r2_between}).}
}
\value{
A list with \code{matches} (data.frame with \code{truth_signal_id},
\code{est_signal_id}, \code{truth_locus_id}, \code{est_locus_id},
\code{representative_to_causal_r2},
\code{representative_to_causal_distance}), \code{missed} (truth signal
ids), \code{extra} (estimated signal ids), \code{locus_matches}
(data.frame with \code{truth_locus_id}, \code{est_locus_id}),
\code{matched_locus_count}, and the normalised \code{truth_signals},
\code{est_signals}, \code{truth_loci} and \code{est_loci} tables.
}
\description{
Loci are matched by chromosome plus interval overlap; estimated
locus intervals are taken from explicit \code{start}/\code{end} columns when
present, parsed from \code{chr:start-end} locus ids otherwise (falling
back to the range of representative positions). Signals within
overlapping loci are matched one-to-one and greedily: candidate
pairs are sorted by r^2 (descending, when \code{G} is supplied), then
representative-to-causal distance (ascending), then marker id, and
each signal is used at most once. Matching never uses signal-id
string equality.
}
\keyword{internal}
