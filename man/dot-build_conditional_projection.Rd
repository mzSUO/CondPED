% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional-scan.R
\name{.build_conditional_projection}
\alias{.build_conditional_projection}
\title{Build the conditional projection for a conditioning genotype set}
\usage{
.build_conditional_projection(
  null_fit,
  X_C = NULL,
  rank_tol = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{null_fit}{A \code{"condped_mt_null"} fit with \code{rotation}.}

\item{X_C}{\code{NULL} or an \verb{n x c} conditioning genotype matrix (or a
length-\code{n} vector for one SNP). Missing dosages are mean-imputed
as in \code{\link[=scan_mt_omnibus]{scan_mt_omnibus()}}.}

\item{rank_tol}{Tolerance forwarded to \code{\link[=.safe_inverse]{.safe_inverse()}}.}
}
\value{
A list with components \code{AM_arr}, \code{Ar}, \code{A_arr} (the fixed
\eqn{V^{-1}} blocks, identical to \code{null_fit$rotation$Vinv}),
\code{G_inv} (pseudo-inverse of the augmented
\eqn{X^{*\top}V^{-1}X^*}), \code{S_XX}, \code{beta_star}, \code{residual_tilde},
\code{U}, \code{lambda}, \code{n_conditioning}, \code{Sigma_G}, \code{Sigma_E}
(references, unchanged), \code{rank_S_XX}, \code{condition_S_XX},
\code{used_pseudoinverse} and \code{status}.
}
\description{
Augments the null-model fixed-effect design with
\eqn{Z_C = I_m \otimes X_C} and constructs the residual-projection
components under the FIXED global-null \eqn{\widehat V}. An empty
\code{X_C} reduces exactly to the original scan projection.
}
\details{
In the eigen-rotated space every per-coordinate block uses the same
\eqn{A_j = V_j^{-1}} blocks from \code{null_fit$rotation$Vinv}; the
augmented design values per coordinate are
\eqn{d_j = (\widetilde w_j, \widetilde x_{C,j})}, giving
\eqn{X^{*\top}V^{-1}X^* = \sum_j d_j d_j^\top \otimes A_j} (in the
trait-fastest \eqn{(a-1)m + t} ordering of the stacked design).
}
\keyword{internal}
