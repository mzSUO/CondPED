% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asset-comparison.R
\name{.run_asset_comparison}
\alias{.run_asset_comparison}
\title{Run one signal's trait-wise effects through ASSET}
\usage{
.run_asset_comparison(
  beta,
  se = NULL,
  z = NULL,
  Sigma_Z,
  trait_names,
  sample_size = NULL,
  two_sided = TRUE,
  backend = NULL
)
}
\arguments{
\item{beta}{Named numeric vector of trait effects for ONE signal
(names must match \code{trait_names}).}

\item{se}{Optional named numeric vector of standard errors
(positive, finite).}

\item{z}{Optional named numeric vector of z-scores; derived as
\code{beta / se} when absent (then \code{se} is required).}

\item{Sigma_Z}{\verb{m x m} correlation matrix of the trait z-scores
(symmetric, unit diagonal, numerically PSD; dimnames must match
\code{trait_names} or be \code{NULL}).}

\item{trait_names}{Trait names (character, unique).}

\item{sample_size}{Total analysed sample size per trait (positive
scalar, or one value per trait); mapped to \code{Neff} in the official
\code{ASSET::fast_asset()} call. Required when \code{backend = NULL} (the
real ASSET path).}

\item{two_sided}{Logical; forwarded to the backend (two-sided subset
search when \code{TRUE}).}

\item{backend}{Test hook: a
\verb{function(beta, se, z, Sigma_Z, trait_names, two_sided)} returning a
raw ASSET-like result (see \code{\link[=.normalize_asset_result]{.normalize_asset_result()}} for the
accepted shapes). When \code{NULL}, the adapter looks for the installed
ASSET package and calls its Z-score subset-search entry point
defensively; any failure yields status \code{"asset_failed"}.}
}
\value{
A list with the frozen comparison schema:
\describe{
\item{asset_p}{Best-subset p-value (\code{NA_real_} unless status is
\code{"ok"}).}
\item{asset_best_subset}{Union of ASSET's positive and negative
subsets (canonical \code{trait_names} order); never interpreted as a
CondPED representative set.}
\item{asset_positive_subset}{Best-subset traits with positive
effects (ASSET's own sign split on the fast_asset path; the
CondPED beta signs for injected backends).}
\item{asset_negative_subset}{Best-subset traits with negative
effects.}
\item{status}{\code{"ok"}, \code{"asset_not_available"} or \code{"asset_failed"}.
Malformed inputs raise \code{condped_invalid_input} instead.}
\item{settings}{List recording \code{scr_pthr} (fixed at the official
default 0.05), \code{two_sided} and \code{Neff}.}
}
}
\description{
Adapter between CondPED per-signal trait-wise estimates and the
external ASSET package. ASSET is an optional dependency: when
\code{backend = NULL} and the package is not installed, the adapter returns
the frozen schema with status \code{"asset_not_available"} instead of
erroring, so comparison pipelines degrade gracefully. The trait
z-score correlation matrix \code{Sigma_Z} is REQUIRED and is always passed
to the backend unchanged — correlated traits are fully supported and
the correlation is never diagonalised away.
}
\keyword{internal}
