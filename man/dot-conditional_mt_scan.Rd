% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional-scan.R
\name{.conditional_mt_scan}
\alias{.conditional_mt_scan}
\title{Conditional multi-trait scan under a fixed conditional projection}
\usage{
.conditional_mt_scan(
  proj,
  G,
  marker_ids = colnames(G),
  rank_tol = sqrt(.Machine$double.eps),
  return_effects = TRUE
)
}
\arguments{
\item{proj}{A projection from \code{\link[=.build_conditional_projection]{.build_conditional_projection()}}.}

\item{G}{\verb{n x p} genotype matrix of candidate SNPs.}

\item{marker_ids}{Marker ids; defaults to \code{colnames(G)}.}

\item{rank_tol}{Tolerance forwarded to \code{\link[=.safe_inverse]{.safe_inverse()}}.}

\item{return_effects}{Logical; also return per-SNP \code{beta}/\code{covariance}.}
}
\value{
A list with \code{conditional} (data.frame: \code{marker_id}, \code{Q},
\code{df}, \code{p_value}, \code{rank_J}, \code{condition_J}, \code{status}), \code{effects}
(list with \code{beta} matrix and \code{covariance} array, or \code{NULL}),
\code{n_conditioning}, \code{status} and \code{diagnostics}.
}
\description{
For each candidate SNP, computes the conditional score
\eqn{U_{j|C} = Z_j^\top P_C y}, information
\eqn{J_{j|C} = Z_j^\top P_C Z_j}, statistic
\eqn{Q_{j|C} = U^\top J^+ U} with \code{df = rank(J)} and the chi-square
p-value, reusing \code{\link[=.gls_block_components]{.gls_block_components()}}. The returned
\code{beta}/\code{covariance} are conditional-scan temporary estimates only
and must not be interpreted as final signal-specific effects.
}
\keyword{internal}
