% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparison-pipelines.R
\name{.pipeline_resolved_asset}
\alias{.pipeline_resolved_asset}
\title{Resolved-ASSET pipeline: final joint signal effects through ASSET}
\usage{
.pipeline_resolved_asset(resolution, trait_names, asset_backend, sample_size)
}
\arguments{
\item{resolution}{A \code{\link[=resolve_locus_signals]{resolve_locus_signals()}} result.}

\item{trait_names}{Trait names (order of \code{fit$Sigma_P_ref}).}

\item{asset_backend}{Backend hook for \code{\link[=.run_asset_comparison]{.run_asset_comparison()}}.}

\item{sample_size}{Total analysed sample size per trait.}
}
\value{
List with \code{signals} (named list per \code{signal_id}; each entry
has \code{locus_id}, \code{signal_id}, \code{representative_snp}, named
\code{beta}/\code{se} vectors, \code{Sigma_Z} and the frozen adapter fields),
\code{n_signals} and \code{status}.
}
\description{
For every RESOLVED signal of a \code{\link[=resolve_locus_signals]{resolve_locus_signals()}} result,
feeds the signal's FINAL JOINT trait-wise beta/se (from the
resolution's \code{beta} long table — never stepwise temporary betas) to
the ASSET adapter. The ASSET correlation matrix is \code{cov2cor()} of
that signal's final joint covariance block.
}
\keyword{internal}
