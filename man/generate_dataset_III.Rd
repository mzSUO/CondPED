% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_data.R
\name{generate_dataset_III}
\alias{generate_dataset_III}
\title{Dataset III：双向因果 A↔B，验证 Class 5}
\usage{
generate_dataset_III(
  n,
  p = 1000L,
  class5_pve = 0.05,
  tau_AB = 0.2,
  tau_BA = 0.2,
  iv_pve = NULL,
  n_iv = 20L,
  rho = 0,
  epi_pairs = NULL,
  epi_effects = NULL,
  maf = 0.3,
  population = "RIL",
  seed = NULL
)
}
\arguments{
\item{n, p}{样本量和SNP数。}

\item{class5_pve}{数值。Class 5位点每个性状的PVE，默认0.05。}

\item{tau_AB}{数值。A→B的因果效应，默认0.2。}

\item{tau_BA}{数值。B→A的因果效应，默认0.2。}

\item{iv_pve}{数值或NULL。IV位点PVE，NULL=自动。}

\item{n_iv}{整数。每个方向的IV位点数，默认20。}

\item{rho, epi_pairs, epi_effects, maf, population, seed}{同其他函数。}
}
\value{
命名列表：Y, G, eps, X, truth, dataset="III", Tau, seed, h2_obs, iv_pve_used。
}
\description{
【数据集设计】
生成模型：M2（双向因果链，联立方程组）
Y_A = tau_BA × Y_B + X × beta_A + eps_A
Y_B = tau_AB × Y_A + X × beta_B + eps_B
}
\details{
【位点布局】
SNP1-2           → Class 5（双向因果）：同时对A和B有直接效应
SNP3-SNP(2+n_iv) → IV_A（A方向工具变量）：仅影响A，用于估计tau_AB
SNP(3+n_iv)-SNP(2+2n_iv) → IV_B（B方向工具变量）：仅影响B，用于估计tau_BA
SNP(3+2n_iv)+    → Null

【IV位点标签说明】
IV_A位点：仅影响A，B的关联通过A→B传递 → 本质上是A→B方向的Class 3
IV_B位点：仅影响B，A的关联通过B→A传递 → 本质上是B→A方向的Class 3
两者均标记为class="class3"，is_IV=TRUE保留供MR使用
}
