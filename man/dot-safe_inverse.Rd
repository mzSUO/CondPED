% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal-utils.R
\name{.safe_inverse}
\alias{.safe_inverse}
\title{Numerically safe matrix inverse with tolerance control}
\usage{
.safe_inverse(A, tol = sqrt(.Machine$double.eps), symmetric = TRUE)
}
\arguments{
\item{A}{Square numeric matrix.}

\item{tol}{Relative tolerance used to decide whether an eigenvalue is
numerically zero. Defaults to \code{sqrt(.Machine$double.eps)}.}

\item{symmetric}{Logical; set \code{TRUE} (default) when \code{A} is known to be
symmetric, enabling the Cholesky fast path and a symmetric eigen
decomposition. When \code{FALSE}, an SVD-based pseudo-inverse is used.}
}
\value{
A list with components:
\describe{
\item{inverse}{The inverse or pseudo-inverse matrix; \code{NULL} on failure.}
\item{rank}{Integer; numerical rank at tolerance \code{tol}.}
\item{eigenvalues}{Numeric vector of eigenvalues (symmetric case) or
singular values (non-symmetric case).}
\item{condition_number}{Ratio of largest to smallest nonzero singular
value/eigenvalue magnitude; \code{Inf} for rank-deficient input.}
\item{method}{\code{"chol"} or \code{"eigen_pinv"}.}
\item{status}{\code{"ok"}, \code{"rank_deficient"} or \code{"failed"}.}
}
}
\description{
Computes the inverse (or Moore-Penrose generalised inverse) of a square
matrix without ever calling an untoleranced \code{\link[=solve]{solve()}}. The numerical rank
is always determined first, from the eigenvalues (symmetric case) or
singular values (non-symmetric case) at relative tolerance \code{tol}; only a
numerically positive definite matrix (full rank with every eigenvalue
significantly positive) takes the Cholesky fast path. Everything else
falls back to a tolerance-controlled eigen/SVD pseudo-inverse. The
Moore-Penrose inverse of a zero matrix is the zero matrix itself,
reported with rank 0 and status \code{"rank_deficient"}.
}
\keyword{internal}
