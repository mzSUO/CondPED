% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/asset-comparison.R
\name{.call_asset_package}
\alias{.call_asset_package}
\title{Call the real ASSET package defensively}
\usage{
.call_asset_package(beta, se, z, Sigma_Z, trait_names, two_sided, sample_size)
}
\arguments{
\item{beta, se, z, Sigma_Z, trait_names, two_sided}{Validated inputs.}

\item{sample_size}{Validated per-trait sample sizes.}
}
\value{
Raw \code{fast_asset()} result (opaque to the caller).
}
\description{
Only reached when \code{backend = NULL} and ASSET is installed. Uses the
official \code{ASSET::fast_asset()} with the frozen mapping
trait_names -> traits.lab, beta -> beta.hat, se -> sigma.hat,
sample_size -> Neff, Sigma_Z -> cor, and blocks built by
\code{ASSET::create_blocks(Sigma_Z)}. \code{scr_pthr} is fixed at the official
default 0.05 (recorded in the output settings).
}
\keyword{internal}
