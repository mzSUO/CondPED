% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate-locus-pve.R
\name{estimate_locus_pve}
\alias{estimate_locus_pve}
\title{Estimate locus-level PVE}
\usage{
estimate_locus_pve(
  effects,
  genotype_variance,
  Sigma_P,
  conditional = NULL,
  correction = c("raw", "noise_corrected", "both"),
  truncate_zero = TRUE,
  gate = TRUE,
  attribution = NULL
)
}
\arguments{
\item{effects}{Effect estimates returned by \code{\link[=estimate_mt_effects]{estimate_mt_effects()}}.}

\item{genotype_variance}{Numeric vector of per-locus genotype variances.}

\item{Sigma_P}{Numeric \verb{m x m} phenotypic covariance matrix used as the
PVE denominator.}

\item{conditional}{Optional conditional-decomposition result returned by
\code{\link[=decompose_conditional_effects]{decompose_conditional_effects()}}.}

\item{correction}{Bias correction to report: \code{"raw"},
\code{"noise_corrected"} or \code{"both"}.}

\item{truncate_zero}{Logical; truncate corrected PVE at zero. The
truncated version does not claim strict unbiasedness.}

\item{gate}{Logical; when \code{TRUE} conditional PVE for non-attributed
traits is not part of the formal output.}

\item{attribution}{Optional attribution result returned by
\code{\link[=attribute_traits]{attribute_traits()}}, required for gating.}
}
\value{
A list with component \code{pve_table} (a data.frame with columns
\code{marker_id}, \code{trait}, \code{pve_raw}, \code{pve_untruncated_bc}, \code{pve_bc},
\code{conditional_pve_raw}, \code{conditional_pve_untruncated_bc},
\code{conditional_pve_bc}, \code{reportable}), plus \code{status} and \code{diagnostics},
as specified in the interface contract.
}
\description{
Estimates the proportion of phenotypic variance explained by each locus
(and, optionally, the conditional PVE per trait), with an optional noise
correction based on \eqn{\widehat\beta^2 - \widehat{s}^2}.
}
\section{Status}{

S0 skeleton: this function is an API stub and currently throws an error of
class \code{condped_not_implemented}.
}

