% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossfit-effect-pve.R
\name{crossfit_effect_pve}
\alias{crossfit_effect_pve}
\title{Cross-fitted effect and PVE estimation}
\usage{
crossfit_effect_pve(
  Y,
  W,
  G,
  fold_id = NULL,
  n_folds = 2L,
  n_repeats = 10L,
  analysis_args = list(),
  gamma_mode = c("global_fixed", "fold_specific"),
  seed = 1L,
  workers = 1L,
  keep_logs = TRUE
)
}
\arguments{
\item{Y}{Numeric \verb{n x m} phenotype matrix.}

\item{W}{Numeric \verb{n x q} fixed-effect design matrix (must explicitly
contain an intercept or be amendable to one).}

\item{G}{Numeric \verb{n x p} genotype dosage matrix coded 0/1/2.}

\item{fold_id}{Optional integer vector of fold assignments; \code{NULL}
generates folds at random.}

\item{n_folds}{Integer; number of folds (default 2).}

\item{n_repeats}{Integer; number of repeated random splits.}

\item{analysis_args}{List of further arguments passed to the analysis
functions.}

\item{gamma_mode}{\code{"global_fixed"} estimates the conditional projection
coefficients once on the full data and keeps them fixed across folds;
\code{"fold_specific"} re-estimates them within each training fold and
reports the two directions separately (never merged).}

\item{seed}{Random seed.}

\item{workers}{Integer; number of parallel workers.}

\item{keep_logs}{Logical; keep per-direction audit logs.}
}
\value{
A list with components \code{fold_results}, \code{aggregate} (data.frame),
\code{audit_log} (a data.frame with columns \code{repeat}, \code{direction},
\code{discovery_ids_hash}, \code{estimation_ids_hash}, \code{gamma_mode},
\code{selected_loci}), \code{status} and \code{diagnostics}, as specified in the
interface contract.
}
\description{
Splits the sample into discovery and estimation folds: the discovery fold
runs the omnibus scan and trait attribution, the estimation fold
re-estimates effects and PVE only at the loci selected in the discovery
fold, mitigating selection bias (winner's curse).
}
\section{Status}{

S0 skeleton: this function is an API stub and currently throws an error of
class \code{condped_not_implemented}.
}

