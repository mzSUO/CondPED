% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_data.R
\name{sim_phenotype_M1}
\alias{sim_phenotype_M1}
\title{单环境表型生成（模型M1，无因果链）}
\usage{
sim_phenotype_M1(
  X,
  B,
  Sigma_E,
  epi_pairs = NULL,
  epi_effects = NULL,
  seed = NULL
)
}
\arguments{
\item{X}{n×p 基因型矩阵。}

\item{B}{p×m 加性效应矩阵。B\link{l_i}=位点l对性状i的效应。}

\item{Sigma_E}{m×m 残差协方差矩阵（由 compute_residual_variance 计算）。}

\item{epi_pairs}{列表，每个元素为 c(l1, l2)，定义一对上位性位点。
NULL=无上位性（默认）。}

\item{epi_effects}{数值矩阵，行数=上位性对数，列数=m。
元素\link{k_i}=第k对位点对性状i的上位性效应 aa_{l1l2,i}。}

\item{seed}{整数或NULL。随机种子。}
}
\value{
命名列表：Y（n×m表型矩阵），G（n×m遗传值），eps（n×m残差）。
}
\description{
实现论文方程 M1：
Y = X × B + eps,   eps ~ MVN(0, Sigma_E)
}
\details{
【上位性支持】
若指定 epi_pairs 和 epi_effects，则在遗传主效应基础上叠加
加性×加性上位性效应（aa项），对应论文 Eq.1 中的 aa_{ilh} 项。
上位性编码：x_l × x_h（两个位点基因型的乘积）

【函数用途】
本函数也被 sim_phenotype_M2 内部调用，生成"基础表型"后再传播因果链。
}
